# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2
chartVersion: 0.15.0
image:
  # registry: ghcr.io/bartaadalbert/kbot
  repository: ghcr.io/bartaadalbert/kbot
  tag: v1.1.12-a945131-linux-amd64
  os: linux
  arch: amd64
secret:
  envName: TG_BOT_TOKEN
  tokenName: kbot
  tokenKey: telegramToken
  tokenKeyValue: 12345678:AAEAKIIIIIIIIIIIIIIIIIIIIIIIIIIIIII
autoscaling:
  enabled: false
serviceAccount:
  create: false
ingress:
  enabled: true
securityContext:
  privileged: true
